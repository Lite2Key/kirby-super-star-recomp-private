// Generated by kssrecomp.generated_blocks. Do not edit.
#include "kss/generated_first_frame_blocks.hpp"
#include "kss/lifted_execution.hpp"
#include "kss/sa1_timing.hpp"

namespace kss::generated {
namespace {
void block_sa1_008bf4_e1m1x1(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008BF4U, true, true, true);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x78, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008BF5U, true, true, true)) { cpu.stopped = true; }
}

void block_sa1_008bf5_e1m1x1(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008BF5U, true, true, true);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x18, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008BF6U, true, true, true)) { cpu.stopped = true; }
}

void block_sa1_008bf6_e1m1x1(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008BF6U, true, true, true);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xFB, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008BF7U, false, true, true)) { cpu.stopped = true; }
}

void block_sa1_008bf7_e0m1x1(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008BF7U, false, true, true);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xE2, {0x20, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008BF9U, false, true, true)) { cpu.stopped = true; }
}

void block_sa1_008bf9_e0m1x1(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008BF9U, false, true, true);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x9C, {0x30, 0x22, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008BFCU, false, true, true)) { cpu.stopped = true; }
}

void block_sa1_008bfc_e0m1x1(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008BFCU, false, true, true);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x9C, {0x09, 0x22, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008BFFU, false, true, true)) { cpu.stopped = true; }
}

void block_sa1_008bff_e0m1x1(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008BFFU, false, true, true);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xA9, {0xFF, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C01U, false, true, true)) { cpu.stopped = true; }
}

void block_sa1_008c01_e0m1x1(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C01U, false, true, true);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x8D, {0x2A, 0x22, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C04U, false, true, true)) { cpu.stopped = true; }
}

void block_sa1_008c04_e0m1x1(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C04U, false, true, true);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x9C, {0x25, 0x22, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C07U, false, true, true)) { cpu.stopped = true; }
}

void block_sa1_008c07_e0m1x1(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C07U, false, true, true);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xA9, {0x80, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C09U, false, true, true)) { cpu.stopped = true; }
}

void block_sa1_008c09_e0m1x1(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C09U, false, true, true);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x8D, {0x27, 0x22, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C0CU, false, true, true)) { cpu.stopped = true; }
}

void block_sa1_008c0c_e0m1x1(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C0CU, false, true, true);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x8D, {0x0A, 0x22, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C0FU, false, true, true)) { cpu.stopped = true; }
}

void block_sa1_008c0f_e0m1x1(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C0FU, false, true, true);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x9C, {0x00, 0x30, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C12U, false, true, true)) { cpu.stopped = true; }
}

void block_sa1_008c12_e0m1x1(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C12U, false, true, true);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x9C, {0x00, 0x60, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C15U, false, true, true)) { cpu.stopped = true; }
}

void block_sa1_008c15_e0m1x1(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C15U, false, true, true);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xC2, {0x30, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C17U, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c17_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C17U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xA2, {0x00, 0x30, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C1AU, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c1a_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C1AU, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xA0, {0x01, 0x30, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C1DU, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c1d_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C1DU, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xA9, {0xFE, 0x07, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C20U, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c20_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C20U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x54, {0x00, 0x00, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C20U, false, false, false) && cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C23U, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c23_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C23U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xA2, {0x00, 0x60, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C26U, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c26_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C26U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xA0, {0x01, 0x60, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C29U, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c29_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C29U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xA9, {0xFE, 0x1E, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C2CU, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c2c_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C2CU, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x54, {0x00, 0x00, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C2CU, false, false, false) && cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C2FU, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c2f_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C2FU, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xA2, {0xFF, 0x37, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C32U, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c32_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C32U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x9A, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C33U, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c33_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C33U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xA9, {0x00, 0x37, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C36U, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c36_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C36U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x5B, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C37U, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c37_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C37U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xA9, {0x77, 0x77, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C3AU, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c3a_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C3AU, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x8D, {0x43, 0x37, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C3DU, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c3d_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C3DU, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xA9, {0x00, 0x20, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C40U, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c40_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C40U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x8D, {0x12, 0x30, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C43U, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c43_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C43U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xA9, {0xC8, 0x11, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C46U, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c46_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C46U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x8D, {0x1B, 0x30, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C49U, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c49_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C49U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xA9, {0xFF, 0xFF, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C4CU, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c4c_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C4CU, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x8D, {0xDC, 0x32, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C4FU, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c4f_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C4FU, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x8D, {0xDE, 0x32, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C52U, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c52_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C52U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x8D, {0x00, 0x30, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C55U, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c55_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C55U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x8D, {0xC8, 0x33, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C58U, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c58_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C58U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xAD, {0x0A, 0x30, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C5BU, false, false, false)) { cpu.stopped = true; }
}

void block_sa1_008c5b_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::sa1, 0x008C5BU, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x10, {0xFB, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    const auto measured = lookup_sa1_reset_timing(expected.address, instruction.opcode);
    const auto mvn_wait = lookup_sa1_reset_mvn_wait(
        expected.address, instruction.opcode, cpu.a, cpu.x, cpu.y);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (measured) { cpu.cycles += measured->observed_wait_cycles; }
    if (mvn_wait) { cpu.cycles += *mvn_wait; }
    if (cpu.block_key() != BlockKey::make(ProcessorId::sa1, 0x008C58U, false, false, false)) { cpu.stopped = true; }
}

void block_scpu_008004_e1m1x1(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x008004U, true, true, true);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x78, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x008005U, true, true, true)) { cpu.stopped = true; }
}

void block_scpu_008005_e1m1x1(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x008005U, true, true, true);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x18, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x008006U, true, true, true)) { cpu.stopped = true; }
}

void block_scpu_008006_e1m1x1(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x008006U, true, true, true);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xFB, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x008007U, false, true, true)) { cpu.stopped = true; }
}

void block_scpu_008007_e0m1x1(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x
_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D55DU, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x5B, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D55EU, false, false, false)) { cpu.stopped = true; }
}

void block_scpu_00d55e_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D55EU, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xAD, {0x40, 0x21, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D561U, false, false, false)) { cpu.stopped = true; }
}

void block_scpu_00d561_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D561U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xC9, {0xAA, 0xBB, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D564U, false, false, false)) { cpu.stopped = true; }
}

void block_scpu_00d564_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D564U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xD0, {0x03, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D566U, false, false, false)) { cpu.stopped = true; }
}

void block_scpu_00d566_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D566U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x20, {0x35, 0xD6, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D635U, false, false, false)) { cpu.stopped = true; }
}

void block_scpu_00d635_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D635U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xA0, {0x00, 0x00, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D638U, false, false, false)) { cpu.stopped = true; }
}

void block_scpu_00d638_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D638U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xA9, {0x3E, 0x0A, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D63BU, false, false, false)) { cpu.stopped = true; }
}

void block_scpu_00d63b_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D63BU, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x85, {0x95, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D63DU, false, false, false)) { cpu.stopped = true; }
}

void block_scpu_00d63d_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D63DU, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xA9, {0xE4, 0x00, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D640U, false, false, false)) { cpu.stopped = true; }
}

void block_scpu_00d640_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D640U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x85, {0x97, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D642U, false, false, false)) { cpu.stopped = true; }
}

void block_scpu_00d642_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D642U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xE2, {0x20, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D644U, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d644_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D644U, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xA9, {0xCC, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D646U, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d646_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D646U, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x80, {0x26, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D66EU, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d648_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D648U, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xB7, {0x95, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D64AU, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d64a_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D64AU, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xC8, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D64BU, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d64b_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D64BU, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xEB, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D64CU, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d64c_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D64CU, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xA9, {0x00, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D64EU, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d64e_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D64EU, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x80, {0x0B, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D65BU, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d650_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D650U, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xEB, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D651U, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d651_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D651U, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xB7, {0x95, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D653U, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d653_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D653U, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xC8, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D654U, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d654_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D654U, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xEB, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D655U, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d655_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D655U, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xCD, {0x40, 0x21, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D658U, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d658_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D658U, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xD0, {0xFB, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D655U, false, true, false) && cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D65AU, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d65a_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D65AU, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x1A, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D65BU, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d65b_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D65BU, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xC2, {0x20, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D65DU, false, false, false)) { cpu.stopped = true; }
}

void block_scpu_00d65d_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D65DU, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x8D, {0x40, 0x21, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D660U, false, false, false)) { cpu.stopped = true; }
}

void block_scpu_00d660_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D660U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xE2, {0x20, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D662U, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d662_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D662U, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xCA, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D663U, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d663_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D663U, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xD0, {0xEB, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D650U, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d66e_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D66EU, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x48, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D66FU, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d66f_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D66FU, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xC2, {0x20, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D671U, false, false, false)) { cpu.stopped = true; }
}

void block_scpu_00d671_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D671U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xB7, {0x95, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D673U, false, false, false)) { cpu.stopped = true; }
}

void block_scpu_00d673_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D673U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xC8, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D674U, false, false, false)) { cpu.stopped = true; }
}

void block_scpu_00d674_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D674U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xC8, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D675U, false, false, false)) { cpu.stopped = true; }
}

void block_scpu_00d675_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D675U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xAA, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D676U, false, false, false)) { cpu.stopped = true; }
}

void block_scpu_00d676_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D676U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xB7, {0x95, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D678U, false, false, false)) { cpu.stopped = true; }
}

void block_scpu_00d678_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D678U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xC8, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D679U, false, false, false)) { cpu.stopped = true; }
}

void block_scpu_00d679_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D679U, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xC8, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D67AU, false, false, false)) { cpu.stopped = true; }
}

void block_scpu_00d67a_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D67AU, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x8D, {0x42, 0x21, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D67DU, false, false, false)) { cpu.stopped = true; }
}

void block_scpu_00d67d_e0m0x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D67DU, false, false, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xE2, {0x20, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D67FU, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d67f_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D67FU, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xE0, {0x01, 0x00, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D682U, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d682_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D682U, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xA9, {0x00, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D684U, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d684_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D684U, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x2A, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D685U, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d685_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D685U, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x8D, {0x41, 0x21, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D688U, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d688_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D688U, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x69, {0x7F, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D68AU, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d68a_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D68AU, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x68, {0x00, 0x00, 0x00}, 0};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D68BU, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d68b_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D68BU, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x8D, {0x40, 0x21, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D68EU, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d68e_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D68EU, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xCD, {0x40, 0x21, 0x00}, 2};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D691U, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d691_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D691U, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0xD0, {0xFB, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D68EU, false, true, false) && cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D693U, false, true, false)) { cpu.stopped = true; }
}

void block_scpu_00d693_e0m1x0(CpuContext& cpu, Bus& bus, Scheduler&) {
    constexpr auto expected = BlockKey::make(ProcessorId::snes_cpu, 0x00D693U, false, true, false);
    if (cpu.block_key() != expected) { cpu.stopped = true; return; }
    const LiftedInstruction instruction{
        0x70, {0xB3, 0x00, 0x00}, 1};
    observe_generated_instruction_fetches(
        bus, cpu.processor, expected.address, instruction.operand_count);
    if (execute_lifted(cpu, bus, instruction).status != LiftStatus::executed) {
        cpu.stopped = true;
        return;
    }
    if (cpu.block_key() != BlockKey::make(ProcessorId::snes_cpu, 0x00D648U, false, true, false)) { cpu.stopped = true; }
}

} // namespace

bool register_first_frame_blocks(CheckedDispatcher& dispatcher) {
    bool ok = true;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008BF4U, true, true, true), &block_sa1_008bf4_e1m1x1) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008BF5U, true, true, true), &block_sa1_008bf5_e1m1x1) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008BF6U, true, true, true), &block_sa1_008bf6_e1m1x1) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008BF7U, false, true, true), &block_sa1_008bf7_e0m1x1) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008BF9U, false, true, true), &block_sa1_008bf9_e0m1x1) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008BFCU, false, true, true), &block_sa1_008bfc_e0m1x1) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008BFFU, false, true, true), &block_sa1_008bff_e0m1x1) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C01U, false, true, true), &block_sa1_008c01_e0m1x1) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C04U, false, true, true), &block_sa1_008c04_e0m1x1) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C07U, false, true, true), &block_sa1_008c07_e0m1x1) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C09U, false, true, true), &block_sa1_008c09_e0m1x1) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C0CU, false, true, true), &block_sa1_008c0c_e0m1x1) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C0FU, false, true, true), &block_sa1_008c0f_e0m1x1) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C12U, false, true, true), &block_sa1_008c12_e0m1x1) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C15U, false, true, true), &block_sa1_008c15_e0m1x1) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C17U, false, false, false), &block_sa1_008c17_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C1AU, false, false, false), &block_sa1_008c1a_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C1DU, false, false, false), &block_sa1_008c1d_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C20U, false, false, false), &block_sa1_008c20_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C23U, false, false, false), &block_sa1_008c23_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C26U, false, false, false), &block_sa1_008c26_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C29U, false, false, false), &block_sa1_008c29_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C2CU, false, false, false), &block_sa1_008c2c_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C2FU, false, false, false), &block_sa1_008c2f_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C32U, false, false, false), &block_sa1_008c32_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C33U, false, false, false), &block_sa1_008c33_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C36U, false, false, false), &block_sa1_008c36_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C37U, false, false, false), &block_sa1_008c37_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C3AU, false, false, false), &block_sa1_008c3a_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C3DU, false, false, false), &block_sa1_008c3d_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C40U, false, false, false), &block_sa1_008c40_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C43U, false, false, false), &block_sa1_008c43_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C46U, false, false, false), &block_sa1_008c46_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C49U, false, false, false), &block_sa1_008c49_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C4CU, false, false, false), &block_sa1_008c4c_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C4FU, false, false, false), &block_sa1_008c4f_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C52U, false, false, false), &block_sa1_008c52_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C55U, false, false, false), &block_sa1_008c55_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C58U, false, false, false), &block_sa1_008c58_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::sa1, 0x008C5BU, false, false, false), &block_sa1_008c5b_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008004U, true, true, true), &block_scpu_008004_e1m1x1) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008005U, true, true, true), &block_scpu_008005_e1m1x1) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008006U, true, true, true), &block_scpu_008006_e1m1x1) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008007U, false, true, true), &block_scpu_008007_e0m1x1) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008009U, false, true, true), &block_scpu_008009_e0m1x1) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00800BU, false, true, false), &block_scpu_00800b_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00800EU, false, true, false), &block_scpu_00800e_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00800FU, false, true, false), &block_scpu_00800f_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008010U, false, true, false), &block_scpu_008010_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008011U, false, true, false), &block_scpu_008011_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008014U, false, true, false), &block_scpu_008014_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008017U, false, true, false), &block_scpu_008017_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008018U, false, true, false), &block_scpu_008018_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00801AU, false, true, false), &block_scpu_00801a_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00801CU, false, true, false), &block_scpu_00801c_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00801EU, false, true, false), &block_scpu_00801e_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008020U, false, true, false), &block_scpu_008020_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008022U, false, true, false), &block_scpu_008022_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008024U, false, true, false), &block_scpu_008024_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008026U, false, true, false), &block_scpu_008026_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008028U, false, true, false), &block_scpu_008028_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00802AU, false, true, false), &block_scpu_00802a_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00802CU, false, true, false), &block_scpu_00802c_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00802EU, false, true, false), &block_scpu_00802e_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008030U, false, true, false), &block_scpu_008030_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008032U, false, true, false), &block_scpu_008032_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008034U, false, true, false), &block_scpu_008034_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008036U, false, true, false), &block_scpu_008036_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008038U, false, true, false), &block_scpu_008038_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00803AU, false, true, false), &block_scpu_00803a_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00803CU, false, true, false), &block_scpu_00803c_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00803EU, false, true, false), &block_scpu_00803e_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008040U, false, true, false), &block_scpu_008040_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008042U, false, true, false), &block_scpu_008042_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008044U, false, true, false), &block_scpu_008044_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008046U, false, true, false), &block_scpu_008046_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008048U, false, true, false), &block_scpu_008048_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00804AU, false, true, false), &block_scpu_00804a_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00804CU, false, true, false), &block_scpu_00804c_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00804EU, false, true, false), &block_scpu_00804e_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008050U, false, true, false), &block_scpu_008050_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008052U, false, true, false), &block_scpu_008052_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008054U, false, true, false), &block_scpu_008054_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008056U, false, true, false), &block_scpu_008056_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008058U, false, true, false), &block_scpu_008058_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00805AU, false, true, false), &block_scpu_00805a_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00805CU, false, true, false), &block_scpu_00805c_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00805EU, false, true, false), &block_scpu_00805e_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008060U, false, true, false), &block_scpu_008060_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008062U, false, true, false), &block_scpu_008062_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008064U, false, true, false), &block_scpu_008064_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008066U, false, true, false), &block_scpu_008066_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008068U, false, true, false), &block_scpu_008068_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00806AU, false, true, false), &block_scpu_00806a_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00806CU, false, true, false), &block_scpu_00806c_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00806EU, false, true, false), &block_scpu_00806e_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008070U, false, true, false), &block_scpu_008070_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008072U, false, true, false), &block_scpu_008072_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008074U, false, true, false), &block_scpu_008074_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008076U, false, true, false), &block_scpu_008076_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008078U, false, true, false), &block_scpu_008078_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00807AU, false, true, false), &block_scpu_00807a_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00807CU, false, true, false), &block_scpu_00807c_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00807EU, false, true, false), &block_scpu_00807e_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008080U, false, true, false), &block_scpu_008080_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008082U, false, true, false), &block_scpu_008082_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008084U, false, true, false), &block_scpu_008084_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008086U, false, true, false), &block_scpu_008086_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008088U, false, true, false), &block_scpu_008088_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00808AU, false, true, false), &block_scpu_00808a_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00808CU, false, true, false), &block_scpu_00808c_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00808EU, false, true, false), &block_scpu_00808e_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008090U, false, true, false), &block_scpu_008090_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008092U, false, true, false), &block_scpu_008092_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008095U, false, true, false), &block_scpu_008095_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008096U, false, true, false), &block_scpu_008096_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008098U, false, true, false), &block_scpu_008098_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00809AU, false, true, false), &block_scpu_00809a_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00809CU, false, true, false), &block_scpu_00809c_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00809EU, false, true, false), &block_scpu_00809e_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080A0U, false, true, false), &block_scpu_0080a0_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080A2U, false, true, false), &block_scpu_0080a2_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080A4U, false, true, false), &block_scpu_0080a4_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080A6U, false, true, false), &block_scpu_0080a6_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080A8U, false, true, false), &block_scpu_0080a8_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080AAU, false, true, false), &block_scpu_0080aa_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080ACU, false, true, false), &block_scpu_0080ac_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080AEU, false, true, false), &block_scpu_0080ae_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080B0U, false, true, false), &block_scpu_0080b0_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080B2U, false, true, false), &block_scpu_0080b2_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080B5U, false, true, false), &block_scpu_0080b5_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080B6U, false, true, false), &block_scpu_0080b6_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080B8U, false, true, false), &block_scpu_0080b8_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080BBU, false, true, false), &block_scpu_0080bb_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080BEU, false, true, false), &block_scpu_0080be_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080C0U, false, true, false), &block_scpu_0080c0_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080C3U, false, true, false), &block_scpu_0080c3_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080C6U, false, true, false), &block_scpu_0080c6_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080C8U, false, true, false), &block_scpu_0080c8_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080CBU, false, true, false), &block_scpu_0080cb_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080CDU, false, true, false), &block_scpu_0080cd_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080D0U, false, true, false), &block_scpu_0080d0_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080D2U, false, true, false), &block_scpu_0080d2_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080D5U, false, true, false), &block_scpu_0080d5_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080D8U, false, true, false), &block_scpu_0080d8_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080DAU, false, true, false), &block_scpu_0080da_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080DDU, false, true, false), &block_scpu_0080dd_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080DFU, false, true, false), &block_scpu_0080df_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080E2U, false, true, false), &block_scpu_0080e2_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080E4U, false, true, false), &block_scpu_0080e4_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080E7U, false, true, false), &block_scpu_0080e7_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080EAU, false, true, false), &block_scpu_0080ea_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080EDU, false, true, false), &block_scpu_0080ed_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080F0U, false, true, false), &block_scpu_0080f0_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080F3U, false, true, false), &block_scpu_0080f3_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080F6U, false, true, false), &block_scpu_0080f6_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080F9U, false, true, false), &block_scpu_0080f9_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080FCU, false, true, false), &block_scpu_0080fc_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x0080FFU, false, true, false), &block_scpu_0080ff_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008101U, false, true, false), &block_scpu_008101_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008104U, false, true, false), &block_scpu_008104_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008107U, false, true, false), &block_scpu_008107_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00810AU, false, true, false), &block_scpu_00810a_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00810DU, false, true, false), &block_scpu_00810d_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00810FU, false, true, false), &block_scpu_00810f_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008112U, false, true, false), &block_scpu_008112_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008115U, false, true, false), &block_scpu_008115_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008118U, false, true, false), &block_scpu_008118_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00811AU, false, true, false), &block_scpu_00811a_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00811DU, false, true, false), &block_scpu_00811d_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008120U, false, true, false), &block_scpu_008120_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008123U, false, true, false), &block_scpu_008123_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008126U, false, true, false), &block_scpu_008126_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008129U, false, true, false), &block_scpu_008129_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00812CU, false, true, false), &block_scpu_00812c_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00812FU, false, true, false), &block_scpu_00812f_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008132U, false, true, false), &block_scpu_008132_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008134U, false, true, false), &block_scpu_008134_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008137U, false, true, false), &block_scpu_008137_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00813AU, false, true, false), &block_scpu_00813a_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00813DU, false, true, false), &block_scpu_00813d_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00813FU, false, true, false), &block_scpu_00813f_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008142U, false, true, false), &block_scpu_008142_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008145U, false, true, false), &block_scpu_008145_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008148U, false, true, false), &block_scpu_008148_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00814BU, false, true, false), &block_scpu_00814b_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00814EU, false, true, false), &block_scpu_00814e_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008151U, false, true, false), &block_scpu_008151_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008154U, false, true, false), &block_scpu_008154_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008156U, false, true, false), &block_scpu_008156_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008159U, false, true, false), &block_scpu_008159_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00815BU, false, true, false), &block_scpu_00815b_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00815EU, false, true, false), &block_scpu_00815e_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008160U, false, true, false), &block_scpu_008160_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008163U, false, true, false), &block_scpu_008163_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008166U, false, true, false), &block_scpu_008166_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008168U, false, true, false), &block_scpu_008168_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00816BU, false, true, false), &block_scpu_00816b_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00816DU, false, false, false), &block_scpu_00816d_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008170U, false, false, false), &block_scpu_008170_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x008172U, false, false, false), &block_scpu_008172_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D559U, false, false, false), &block_scpu_00d559_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D55AU, false, false, false), &block_scpu_00d55a_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D55DU, false, false, false), &block_scpu_00d55d_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D55EU, false, false, false), &block_scpu_00d55e_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D561U, false, false, false), &block_scpu_00d561_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D564U, false, false, false), &block_scpu_00d564_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D566U, false, false, false), &block_scpu_00d566_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D635U, false, false, false), &block_scpu_00d635_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D638U, false, false, false), &block_scpu_00d638_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D63BU, false, false, false), &block_scpu_00d63b_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D63DU, false, false, false), &block_scpu_00d63d_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D640U, false, false, false), &block_scpu_00d640_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D642U, false, false, false), &block_scpu_00d642_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D644U, false, true, false), &block_scpu_00d644_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D646U, false, true, false), &block_scpu_00d646_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D648U, false, true, false), &block_scpu_00d648_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D64AU, false, true, false), &block_scpu_00d64a_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D64BU, false, true, false), &block_scpu_00d64b_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D64CU, false, true, false), &block_scpu_00d64c_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D64EU, false, true, false), &block_scpu_00d64e_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D650U, false, true, false), &block_scpu_00d650_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D651U, false, true, false), &block_scpu_00d651_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D653U, false, true, false), &block_scpu_00d653_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D654U, false, true, false), &block_scpu_00d654_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D655U, false, true, false), &block_scpu_00d655_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D658U, false, true, false), &block_scpu_00d658_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D65AU, false, true, false), &block_scpu_00d65a_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D65BU, false, true, false), &block_scpu_00d65b_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D65DU, false, false, false), &block_scpu_00d65d_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D660U, false, false, false), &block_scpu_00d660_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D662U, false, true, false), &block_scpu_00d662_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D663U, false, true, false), &block_scpu_00d663_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D66EU, false, true, false), &block_scpu_00d66e_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D66FU, false, true, false), &block_scpu_00d66f_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D671U, false, false, false), &block_scpu_00d671_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D673U, false, false, false), &block_scpu_00d673_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D674U, false, false, false), &block_scpu_00d674_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D675U, false, false, false), &block_scpu_00d675_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D676U, false, false, false), &block_scpu_00d676_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D678U, false, false, false), &block_scpu_00d678_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D679U, false, false, false), &block_scpu_00d679_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D67AU, false, false, false), &block_scpu_00d67a_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D67DU, false, false, false), &block_scpu_00d67d_e0m0x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D67FU, false, true, false), &block_scpu_00d67f_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D682U, false, true, false), &block_scpu_00d682_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D684U, false, true, false), &block_scpu_00d684_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D685U, false, true, false), &block_scpu_00d685_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D688U, false, true, false), &block_scpu_00d688_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D68AU, false, true, false), &block_scpu_00d68a_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D68BU, false, true, false), &block_scpu_00d68b_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D68EU, false, true, false), &block_scpu_00d68e_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D691U, false, true, false), &block_scpu_00d691_e0m1x0) == RegistrationStatus::registered && ok;
    ok = dispatcher.register_block(BlockKey::make(ProcessorId::snes_cpu, 0x00D693U, false, true, false), &block_scpu_00d693_e0m1x0) == RegistrationStatus::registered && ok;
    return ok;
}

std::size_t first_frame_block_count() noexcept { return 254U; }

} // namespace kss::generated
