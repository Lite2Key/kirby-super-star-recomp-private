# Bootstrap progress tests

- Evidence ID: `bootstrap.tests`
- Command: `python -m unittest discover -s tests/progress -v`
- Result: 10 tests passed
- Coverage: schema invariants, dashboard generation, denominator honesty, evidence-required green gates, safe JSON embedding, repository snapshot derivation, evidence upserts, and deterministic stale checks
- Nintendo-derived data: none
